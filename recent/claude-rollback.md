
https://salesforce.stackexchange.com/questions/417618/rollbackback-promotechanges-through-sfdx-git-delta-deploy

But the technique implied by the title — using `sfdx-git-delta` to both "promote" and "rollback" — matches exactly the bidirectional trick I described earlier: `sgd` doesn't care which ref is "before" and which is "after," so generating a rollback package is just calling it with `--from` and `--to` reversed relative to your normal forward deploy. That's a real, well-documented capability of the tool, not a hack — and it's a good anchor for the comparison you're asking for.

Here's the breakdown, decomposing rollback into its actual components rather than treating it as one feature:

| Component | Gearset's likely strategy | Native DIY rebuild (effort) | Git-delta reversal workaround (effort) | Gap in the workaround |
|---|---|---|---|---|
| **1. State capture** (pre-deploy snapshot) | Full/scoped metadata retrieve of the target org taken automatically before every deployment, stored centrally | **High** — `sf project retrieve start` before each deploy + a storage/indexing layer (3–6 weeks) | **None needed** — the previous git commit already *is* the snapshot, free | Doesn't capture org drift (manual changes made outside Git) — the single biggest gap |
| **2. Identifying what to revert** | Stored manifest per deployment in their history DB, browsable by the user | **Medium** — needs a deployment ledger (commit SHA ↔ deploy ID mapping), ~1–2 weeks | **Low** — the before/after commit SHAs from the merge already identify it, free | Breaks down if history is squashed/rebased — "what exactly got deployed" gets blurry |
| **3. Computing the inverse package** | Their org-comparison engine run in reverse (snapshot vs current org state) | **High** — rebuilding a metadata diff engine against two *org states*, 1–3+ months | **Low** — `sgd --from <bad-commit> --to <good-commit>` generates the inverse `package.xml`/`destructiveChanges.xml` automatically; a few hours to wire as a workflow | Computed against git history, not live org state — wrong if anything else touched the org in between |
| **4. Re-running dependency checks on the rollback package** | Problem Analyzer re-run against the rollback package before deploying | **Medium** — reuse your Tier-A checks (`deploy validate --dry-run`, lightning-flow-scanner) | **Low** — same reuse, essentially free if Tier A already exists | None significant — this part transfers cleanly either way |
| **5. Un-revertible metadata** (Metadata API limits) | Documented exception list, surfaced as a warning, not solved | **Low–Medium** — just inherit and document the same platform limits | **Low** — identical limitation, same warning needed | None — this is a Salesforce platform constraint, not a tooling difference |
| **6. Deploying the rollback** | Same deploy engine, tagged `Type: Rollback` | **Low** — same `sf project deploy start` call | **Low** — identical | None |
| **7. Audit trail / history UI** | Deployment history page, downloadable PDF report | **Medium** — minimal ledger/dashboard, 3–5 days | **Low** — GH Actions run log + git history is your audit trail, free but unpolished | No report generation, no nice UI — acceptable trade for most internal teams |
| **8. "Roll back the rollback"** | Explicitly supported | **Low** — falls out for free if step 3's logic is generic | **Low** — run the same `sgd` trick again with refs flipped again, falls out naturally | None |
| **9. Snapshot storage & retention** | Managed cloud storage, effectively unlimited history | **Medium–High** — GH Actions artifacts expire (~90 days default), so long retention needs Release assets/external storage, ~1 week + ongoing | **~Zero** — git history is the storage, free, no extra system | None — this is actually a clean win for the workaround |
| **10. One-click UX** (partial selection, validate-then-deploy) | Polished web UI | **Medium–High** — building UI is the expensive, usually unnecessary part, 3–5 days for a bare form | **Low** — a `workflow_dispatch` form with a commit-SHA input covers 90% of real usage | Less polish, no partial-item picker without extra scripting |

### What this table actually shows

The genuinely hard 30% of Gearset's rollback — items **1, 3, and 9** — all stem from one design decision: comparing against *live org state*, not git history. That's what lets them roll back a change even if someone made unrelated clicks in Production afterward, and it's what makes their engine expensive to rebuild (months, not weeks).

The Stack Exchange workaround sidesteps that decision entirely. It redefines "rollback" as **"redeploy the org to match a previous git commit"** rather than **"undo exactly this deployment regardless of what happened since."** For a team with strict git discipline (nothing ever deployed or clicked outside the pipeline), those two definitions converge and the cheap version is functionally equivalent — which is exactly why it's "far more straightforward": it's solving an easier problem, not a cheaper version of the same problem. The moment an admin makes an out-of-band change in Production, the two diverge, and only Gearset's org-state-aware version notices.
